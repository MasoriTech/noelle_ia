# Noelle Companion V16

Pack de restauração de integração para Electron.

Use apenas:

```bat
INICIAR.bat
```

A opção 1 verifica dependências npm, `.venv`, STT, TTS, Ollama, modelo `qwen3:0.6b`, manifests e assets antes de iniciar.
