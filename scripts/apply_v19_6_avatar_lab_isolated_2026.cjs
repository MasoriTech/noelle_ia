"use strict";

const fs = require("fs");
const path = require("path");
const cp = require("child_process");

const ROOT = process.cwd();
const BACKUP_ROOT = path.join(ROOT, "backups", "v19_6_avatar_lab_isolated_" + new Date().toISOString().replace(/[:.]/g, "-").replace("T", "_").slice(0, 19));

const FILES = {
  avatarLabHtml: "<!doctype html>\n<html lang=\"pt-BR\">\n<head>\n  <meta charset=\"utf-8\" />\n  <meta http-equiv=\"Content-Security-Policy\" content=\"default-src 'self' file: blob: data:; script-src 'self' 'unsafe-inline' file: blob:; style-src 'self' 'unsafe-inline'; img-src 'self' file: blob: data:; media-src 'self' file: blob: data:; connect-src 'self' file: blob: data:;\" />\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" />\n  <title>Noelle Avatar Lab V19.6</title>\n  <style>\n    :root {\n      color-scheme: dark;\n      --bg: #080812;\n      --card: rgba(15, 15, 28, .94);\n      --line: rgba(255, 83, 136, .28);\n      --text: #fff4fb;\n      --muted: #c8b8d4;\n      --accent: #ff477e;\n      --accent2: #8b5cf6;\n      --ok: #42f5ad;\n      --warn: #ffd166;\n      --danger: #ff6b6b;\n    }\n    * { box-sizing: border-box; }\n    body {\n      margin: 0;\n      background: radial-gradient(circle at 20% 0%, rgba(255,71,126,.18), transparent 32%), var(--bg);\n      color: var(--text);\n      font-family: Inter, Segoe UI, system-ui, Arial, sans-serif;\n      overflow: hidden;\n    }\n    .app {\n      height: 100vh;\n      display: grid;\n      grid-template-rows: auto 1fr auto;\n    }\n    .topbar, .downbar {\n      display: flex;\n      align-items: center;\n      gap: 10px;\n      padding: 12px 14px;\n      border-bottom: 1px solid var(--line);\n      background: rgba(8, 8, 18, .88);\n      backdrop-filter: blur(12px);\n    }\n    .downbar {\n      border-top: 1px solid var(--line);\n      border-bottom: none;\n      color: var(--muted);\n      font-size: 13px;\n    }\n    .topbar h1 {\n      font-size: 18px;\n      margin: 0;\n      white-space: nowrap;\n    }\n    .pill {\n      display: inline-flex;\n      border: 1px solid rgba(255,255,255,.14);\n      border-radius: 999px;\n      padding: 5px 9px;\n      color: var(--muted);\n      background: rgba(255,255,255,.04);\n      font-size: 13px;\n    }\n    .pill.ok { color: var(--ok); border-color: rgba(66,245,173,.35); }\n    .pill.warn { color: var(--warn); border-color: rgba(255,209,102,.35); }\n    .pill.danger { color: var(--danger); border-color: rgba(255,107,107,.35); }\n    .main {\n      min-height: 0;\n      display: grid;\n      grid-template-columns: minmax(420px, 1fr) 360px;\n      gap: 12px;\n      padding: 12px;\n    }\n    @media (max-width: 980px) {\n      body { overflow: auto; }\n      .app { height: auto; min-height: 100vh; }\n      .main { grid-template-columns: 1fr; }\n    }\n    .stage, .side {\n      min-height: 0;\n      border: 1px solid var(--line);\n      background: var(--card);\n      border-radius: 22px;\n      overflow: hidden;\n      box-shadow: 0 16px 42px rgba(0,0,0,.22);\n    }\n    .stage { position: relative; }\n    #avatarCanvas {\n      width: 100%;\n      height: 100%;\n      display: block;\n      min-height: 520px;\n      background: radial-gradient(circle at 50% 30%, rgba(255,255,255,.09), transparent 24%), #11111d;\n    }\n    .overlay {\n      position: absolute;\n      left: 14px;\n      top: 14px;\n      display: flex;\n      gap: 8px;\n      flex-wrap: wrap;\n      max-width: calc(100% - 28px);\n      pointer-events: none;\n    }\n    .side {\n      padding: 14px;\n      overflow: auto;\n    }\n    .card {\n      border: 1px solid rgba(255,255,255,.09);\n      border-radius: 18px;\n      padding: 13px;\n      background: rgba(255,255,255,.04);\n      margin-bottom: 12px;\n    }\n    .card h2 {\n      margin: 0 0 10px;\n      font-size: 16px;\n    }\n    label {\n      color: var(--muted);\n      font-weight: 700;\n      font-size: 13px;\n    }\n    select, input[type=\"file\"], input[type=\"range\"] {\n      width: 100%;\n      color-scheme: dark;\n      background: #211f2d;\n      color: var(--text);\n      border: 1px solid rgba(255,255,255,.14);\n      border-radius: 12px;\n      padding: 9px 10px;\n      margin: 6px 0 10px;\n    }\n    select option { background: #211f2d; color: var(--text); }\n    button {\n      border: 1px solid rgba(255,255,255,.14);\n      color: var(--text);\n      background: rgba(255,255,255,.08);\n      border-radius: 13px;\n      padding: 9px 12px;\n      font-weight: 850;\n      cursor: pointer;\n      margin: 3px;\n    }\n    button.primary {\n      background: linear-gradient(135deg, var(--accent), var(--accent2));\n    }\n    .buttons { display: flex; flex-wrap: wrap; gap: 4px; }\n    .debug {\n      max-height: 190px;\n      overflow: auto;\n      white-space: pre-wrap;\n      font-family: ui-monospace, Consolas, monospace;\n      font-size: 12px;\n      color: var(--muted);\n      background: rgba(0,0,0,.24);\n      border-radius: 12px;\n      padding: 10px;\n    }\n    .kbd {\n      display: inline-block;\n      border-radius: 8px;\n      background: rgba(255,255,255,.09);\n      color: var(--text);\n      padding: 2px 7px;\n      font-family: ui-monospace, Consolas, monospace;\n      word-break: break-all;\n    }\n  </style>\n</head>\n<body>\n  <div class=\"app\">\n    <header class=\"topbar\">\n      <h1>\ud83e\uddea Noelle Avatar Lab V19.6</h1>\n      <span class=\"pill warn\" id=\"statusPill\">Inicializando...</span>\n      <span class=\"pill\" id=\"syncPill\">Room sync: aguardando</span>\n    </header>\n\n    <main class=\"main\">\n      <section class=\"stage\">\n        <canvas id=\"avatarCanvas\"></canvas>\n        <div class=\"overlay\" id=\"overlayPills\"></div>\n      </section>\n\n      <aside class=\"side\">\n        <section class=\"card\">\n          <h2>1. Preview real do VRM</h2>\n          <label>Avatar do projeto</label>\n          <select id=\"avatarSelect\">\n            <option value=\"./assets/Noelle.vrm\">Noelle principal \u2014 src/assets/Noelle.vrm</option>\n            <option value=\"./assets/avatars/Yoru.vrm\">Yoru \u2014 src/assets/avatars/Yoru.vrm</option>\n            <option value=\"./assets/avatars/Noelle.vrm\">Noelle avatars \u2014 src/assets/avatars/Noelle.vrm</option>\n            <option value=\"./assets/avatars/yoru.vrm\">yoru.vrm min\u00fasculo</option>\n          </select>\n          <label>Ou carregar VRM/GLB local</label>\n          <input id=\"localFile\" type=\"file\" accept=\".vrm,.glb,.gltf\" />\n          <div class=\"buttons\">\n            <button class=\"primary\" id=\"btnLoad\">Carregar avatar</button>\n            <button id=\"btnResetCamera\">Reset c\u00e2mera</button>\n          </div>\n        </section>\n\n        <section class=\"card\">\n          <h2>2. Loader novo + express\u00f5es</h2>\n          <div class=\"buttons\">\n            <button id=\"btnIdle\">Idle</button>\n            <button id=\"btnBlink\">Blink</button>\n            <button id=\"btnHappy\">Happy</button>\n            <button id=\"btnNeutral\">Neutral</button>\n          </div>\n          <label>Zoom</label>\n          <input id=\"zoomRange\" type=\"range\" min=\"2.2\" max=\"6.5\" step=\"0.1\" value=\"4.2\" />\n        </section>\n\n        <section class=\"card\">\n          <h2>3. Anima\u00e7\u00f5es VRM/VRMA</h2>\n          <label>Motion VRMA</label>\n          <select id=\"motionSelect\">\n            <option value=\"\">Sem motion / idle simples</option>\n          </select>\n          <div class=\"buttons\">\n            <button class=\"primary\" id=\"btnPlayMotion\">Tocar VRMA</button>\n            <button id=\"btnStopMotion\">Parar motion</button>\n          </div>\n        </section>\n\n        <section class=\"card\">\n          <h2>4. Sincroniza\u00e7\u00e3o com Room</h2>\n          <p>Envia avatar, expression e motion por <span class=\"kbd\">BroadcastChannel</span> e <span class=\"kbd\">localStorage</span>.</p>\n          <div class=\"buttons\">\n            <button class=\"primary\" id=\"btnSyncRoom\">Sincronizar Room</button>\n            <button id=\"btnCopyReport\">Copiar relat\u00f3rio</button>\n          </div>\n        </section>\n\n        <section class=\"card\">\n          <h2>Debug</h2>\n          <pre class=\"debug\" id=\"debugBox\">Aguardando...</pre>\n        </section>\n      </aside>\n    </main>\n\n    <footer class=\"downbar\">\n      <span>N\u00e3o injeta MutationObserver no app principal.</span>\n      <span>\u2022</span>\n      <span>Se asset por caminho falhar, use \u201ccarregar VRM local\u201d.</span>\n    </footer>\n  </div>\n\n  <script src=\"./renderer_dist/avatar_lab_v19_6.bundle.js\"></script>\n</body>\n</html>\n",
  avatarLabApp: "import * as THREE from \"three\";\nimport { GLTFLoader } from \"three/examples/jsm/loaders/GLTFLoader.js\";\nimport { OrbitControls } from \"three/examples/jsm/controls/OrbitControls.js\";\nimport { VRMLoaderPlugin, VRMUtils } from \"@pixiv/three-vrm\";\nimport { VRMAnimationLoaderPlugin, createVRMAnimationClip } from \"@pixiv/three-vrm-animation\";\n\nconst els = {\n  canvas: document.getElementById(\"avatarCanvas\"),\n  statusPill: document.getElementById(\"statusPill\"),\n  syncPill: document.getElementById(\"syncPill\"),\n  overlay: document.getElementById(\"overlayPills\"),\n  debug: document.getElementById(\"debugBox\"),\n  avatarSelect: document.getElementById(\"avatarSelect\"),\n  localFile: document.getElementById(\"localFile\"),\n  motionSelect: document.getElementById(\"motionSelect\"),\n  zoomRange: document.getElementById(\"zoomRange\"),\n  btnLoad: document.getElementById(\"btnLoad\"),\n  btnResetCamera: document.getElementById(\"btnResetCamera\"),\n  btnIdle: document.getElementById(\"btnIdle\"),\n  btnBlink: document.getElementById(\"btnBlink\"),\n  btnHappy: document.getElementById(\"btnHappy\"),\n  btnNeutral: document.getElementById(\"btnNeutral\"),\n  btnPlayMotion: document.getElementById(\"btnPlayMotion\"),\n  btnStopMotion: document.getElementById(\"btnStopMotion\"),\n  btnSyncRoom: document.getElementById(\"btnSyncRoom\"),\n  btnCopyReport: document.getElementById(\"btnCopyReport\")\n};\n\nconst state = {\n  currentRoot: null,\n  currentVRM: null,\n  currentUrl: \"\",\n  mixer: null,\n  idleTime: 0,\n  selectedMotion: \"\",\n  lastSync: null,\n  objectUrls: []\n};\n\nlet channel = null;\n\nfunction log(message) {\n  const line = `[${new Date().toLocaleTimeString()}] ${message}`;\n  els.debug.textContent = `${line}\\n${els.debug.textContent || \"\"}`.slice(0, 6000);\n  console.log(\"[Avatar Lab V19.6]\", message);\n}\n\nfunction setStatus(text, type = \"warn\") {\n  els.statusPill.textContent = text;\n  els.statusPill.className = `pill ${type}`;\n  log(text);\n}\n\nfunction setOverlay(items) {\n  els.overlay.innerHTML = \"\";\n  for (const item of items) {\n    const span = document.createElement(\"span\");\n    span.className = `pill ${item.type || \"\"}`;\n    span.textContent = item.text;\n    els.overlay.appendChild(span);\n  }\n}\n\nfunction getChannel() {\n  if (channel) return channel;\n  try {\n    channel = new BroadcastChannel(\"noelle-avatar-room-sync\");\n  } catch {\n    channel = null;\n  }\n  return channel;\n}\n\nfunction postSync(type, payload = {}) {\n  const message = {\n    source: \"avatar-lab-v19-6\",\n    type,\n    payload,\n    at: new Date().toISOString()\n  };\n\n  state.lastSync = message;\n\n  try { getChannel()?.postMessage(message); } catch {}\n  try { localStorage.setItem(\"noelle.avatar.sync.state\", JSON.stringify(message, null, 2)); } catch {}\n  try { window.dispatchEvent(new CustomEvent(\"noelle:avatar-sync\", { detail: message })); } catch {}\n\n  els.syncPill.textContent = `Room sync: ${type}`;\n  els.syncPill.className = \"pill ok\";\n  log(`Sync enviado: ${type}`);\n}\n\nfunction absUrl(url) {\n  if (url.startsWith(\"blob:\") || url.startsWith(\"data:\") || url.startsWith(\"file:\")) return url;\n  return new URL(url, document.baseURI).href;\n}\n\nfunction createVRMLoader() {\n  const loader = new GLTFLoader();\n  loader.setCrossOrigin?.(\"anonymous\");\n  loader.register((parser) => new VRMLoaderPlugin(parser));\n  return loader;\n}\n\nfunction createVRMALoader() {\n  const loader = new GLTFLoader();\n  loader.setCrossOrigin?.(\"anonymous\");\n  loader.register((parser) => new VRMAnimationLoaderPlugin(parser));\n  return loader;\n}\n\nfunction disposeObject(object) {\n  object?.traverse?.((child) => {\n    if (!child.isMesh) return;\n    child.geometry?.dispose?.();\n    const materials = Array.isArray(child.material) ? child.material : [child.material];\n    for (const material of materials.filter(Boolean)) {\n      for (const key of Object.keys(material)) {\n        const value = material[key];\n        if (value?.isTexture) value.dispose?.();\n      }\n      material.dispose?.();\n    }\n  });\n}\n\nfunction normalizeModel(root, targetHeight = 1.62) {\n  root.updateMatrixWorld(true);\n  const box = new THREE.Box3().setFromObject(root);\n  if (box.isEmpty()) return;\n\n  const size = new THREE.Vector3();\n  const center = new THREE.Vector3();\n  box.getSize(size);\n  box.getCenter(center);\n\n  root.position.x -= center.x;\n  root.position.z -= center.z;\n  root.position.y -= box.min.y;\n\n  const scale = targetHeight / Math.max(0.01, size.y);\n  root.scale.setScalar(Math.max(0.001, Math.min(20, scale)));\n}\n\nconst renderer = new THREE.WebGLRenderer({\n  canvas: els.canvas,\n  antialias: true,\n  alpha: true,\n  powerPreference: \"high-performance\"\n});\nrenderer.setPixelRatio(Math.min(window.devicePixelRatio || 1, 2));\nrenderer.outputColorSpace = THREE.SRGBColorSpace;\nrenderer.shadowMap.enabled = true;\n\nconst scene = new THREE.Scene();\nscene.background = new THREE.Color(0x10101c);\n\nconst camera = new THREE.PerspectiveCamera(30, 1, 0.1, 80);\ncamera.position.set(0, 1.25, 4.2);\n\nconst controls = new OrbitControls(camera, els.canvas);\ncontrols.target.set(0, 1.0, 0);\ncontrols.enableDamping = true;\n\nscene.add(new THREE.HemisphereLight(0xffffff, 0x242435, 1.25));\nconst key = new THREE.DirectionalLight(0xffffff, 1.4);\nkey.position.set(2, 4, 3);\nkey.castShadow = true;\nscene.add(key);\n\nconst grid = new THREE.GridHelper(4, 20, 0xff477e, 0x39394a);\nscene.add(grid);\n\nconst clock = new THREE.Clock();\n\nfunction resize() {\n  const rect = els.canvas.getBoundingClientRect();\n  const width = Math.max(1, Math.floor(rect.width));\n  const height = Math.max(1, Math.floor(rect.height));\n  renderer.setSize(width, height, false);\n  camera.aspect = width / height;\n  camera.updateProjectionMatrix();\n}\n\nnew ResizeObserver(resize).observe(els.canvas);\nresize();\n\nfunction resetCamera() {\n  camera.position.set(0, 1.25, Number(els.zoomRange.value || 4.2));\n  controls.target.set(0, 1.0, 0);\n  controls.update();\n}\n\nfunction clearAvatar() {\n  if (state.currentRoot) {\n    scene.remove(state.currentRoot);\n    disposeObject(state.currentRoot);\n  }\n  state.currentRoot = null;\n  state.currentVRM = null;\n  state.mixer = null;\n}\n\nasync function loadAvatar(url, label = url) {\n  const finalUrl = absUrl(url);\n  setStatus(`Carregando ${label}...`, \"warn\");\n\n  clearAvatar();\n\n  const loader = createVRMLoader();\n  const gltf = await loader.loadAsync(finalUrl);\n\n  const vrm = gltf?.userData?.vrm || null;\n  const vrmScene = vrm?.scene || gltf.scene || gltf.scenes?.[0];\n\n  if (!vrmScene) throw new Error(\"Arquivo carregou sem vrm.scene/scene\");\n\n  if (vrm) {\n    try { VRMUtils.removeUnnecessaryVertices?.(vrmScene); } catch {}\n    try { VRMUtils.removeUnnecessaryJoints?.(vrmScene); } catch {}\n    try { VRMUtils.rotateVRM0?.(vrm); } catch {}\n  }\n\n  vrmScene.traverse((child) => {\n    if (!child.isMesh) return;\n    child.castShadow = true;\n    child.receiveShadow = true;\n    child.frustumCulled = false;\n    const materials = Array.isArray(child.material) ? child.material : [child.material];\n    for (const material of materials.filter(Boolean)) {\n      material.needsUpdate = true;\n      if (material.map) material.map.colorSpace = THREE.SRGBColorSpace;\n    }\n  });\n\n  const wrapper = new THREE.Group();\n  wrapper.name = \"AvatarLabV196Root\";\n  wrapper.add(vrmScene);\n  normalizeModel(wrapper, 1.62);\n  scene.add(wrapper);\n\n  state.currentRoot = wrapper;\n  state.currentVRM = vrm;\n  state.currentUrl = label;\n  state.mixer = new THREE.AnimationMixer(wrapper);\n  state.idleTime = 0;\n\n  setOverlay([\n    { text: vrm ? \"VRM ativo\" : \"GLB fallback\", type: vrm ? \"ok\" : \"warn\" },\n    { text: label.split(\"/\").pop(), type: \"ok\" }\n  ]);\n  setStatus(vrm ? `VRM ativo: ${label}` : `Modelo ativo: ${label}`, vrm ? \"ok\" : \"warn\");\n  postSync(\"avatar:loaded\", { url: label, hasVRM: !!vrm });\n}\n\nfunction setExpression(name, value = 1) {\n  const manager = state.currentVRM?.expressionManager;\n  if (!manager) {\n    setStatus(\"expressionManager n\u00e3o dispon\u00edvel neste VRM\", \"warn\");\n    return;\n  }\n\n  const names = [\"happy\", \"relaxed\", \"angry\", \"sad\", \"aa\", \"ih\", \"ou\", \"ee\", \"oh\", \"blink\", \"blinkLeft\", \"blinkRight\", \"neutral\"];\n  for (const n of names) {\n    try { manager.setValue(n, 0); } catch {}\n  }\n\n  if (name !== \"neutral\") {\n    try { manager.setValue(name, value); } catch {}\n  }\n  try { manager.update?.(); } catch {}\n\n  setStatus(`Expression: ${name}`, \"ok\");\n  postSync(\"expression:set\", { name, value });\n}\n\nfunction stopMotion() {\n  state.mixer?.stopAllAction?.();\n  setStatus(\"Motion parada\", \"ok\");\n  postSync(\"motion:stop\", {});\n}\n\nasync function playMotion(url) {\n  if (!state.currentVRM) {\n    setStatus(\"Carregue um VRM antes de tocar VRMA\", \"warn\");\n    return;\n  }\n  if (!url) {\n    setStatus(\"Nenhuma motion selecionada\", \"warn\");\n    return;\n  }\n\n  const finalUrl = absUrl(url);\n  setStatus(`Carregando motion ${url.split(\"/\").pop()}...`, \"warn\");\n\n  const loader = createVRMALoader();\n  const gltf = await loader.loadAsync(finalUrl);\n  const vrmAnimation = gltf?.userData?.vrmAnimations?.[0];\n\n  if (!vrmAnimation) throw new Error(\"VRMA sem userData.vrmAnimations[0]\");\n\n  const clip = createVRMAnimationClip(vrmAnimation, state.currentVRM);\n  state.mixer?.stopAllAction?.();\n\n  const action = state.mixer.clipAction(clip);\n  action.reset().play();\n\n  setStatus(`Motion ativa: ${url.split(\"/\").pop()}`, \"ok\");\n  postSync(\"motion:play\", { url });\n}\n\nfunction collectMotions(value, out) {\n  if (!value) return;\n  if (typeof value === \"string\" && value.toLowerCase().endsWith(\".vrma\")) {\n    out.push(value);\n    return;\n  }\n  if (Array.isArray(value)) {\n    for (const item of value) collectMotions(item, out);\n    return;\n  }\n  if (typeof value === \"object\") {\n    for (const key of [\"path\", \"file\", \"src\", \"url\", \"motion\", \"vrma\"]) collectMotions(value[key], out);\n    for (const item of Object.values(value)) if (typeof item === \"object\") collectMotions(item, out);\n  }\n}\n\nfunction normalizeMotionPath(path) {\n  let p = String(path).replace(/\\\\/g, \"/\");\n  if (p.startsWith(\"./\")) return p;\n  if (p.startsWith(\"src/assets/\")) return \"./assets/\" + p.slice(\"src/assets/\".length);\n  if (p.startsWith(\"assets/\")) return \"./\" + p;\n  if (p.startsWith(\"motions/\")) return \"./assets/\" + p;\n  return \"./assets/motions/\" + p.split(\"/\").pop();\n}\n\nasync function loadMotionManifest() {\n  const candidates = [\n    \"./assets/motion_manifest.json\",\n    \"./assets/motions/motion_manifest.json\",\n    \"./assets/room_manifest.json\"\n  ];\n  const found = [];\n\n  for (const url of candidates) {\n    try {\n      const res = await fetch(absUrl(url), { cache: \"no-store\" });\n      if (!res.ok) continue;\n      collectMotions(await res.json(), found);\n      log(`Manifest lido: ${url}`);\n    } catch (err) {\n      log(`Manifest indispon\u00edvel: ${url}`);\n    }\n  }\n\n  let motions = [...new Set(found.map(normalizeMotionPath))];\n\n  if (!motions.length) {\n    motions = [\n      \"./assets/motions/001_motion_pose.vrma\",\n      \"./assets/motions/004_hello_1.vrma\",\n      \"./assets/motions/006_drinkwater.vrma\",\n      \"./assets/motions/VRMA_01.vrma\"\n    ];\n  }\n\n  for (const url of motions.slice(0, 80)) {\n    const option = document.createElement(\"option\");\n    option.value = url;\n    option.textContent = url.split(\"/\").pop();\n    els.motionSelect.appendChild(option);\n  }\n\n  log(`Motions VRMA dispon\u00edveis: ${motions.length}`);\n}\n\nfunction copyReport() {\n  const report = {\n    patch: \"Noelle V19.6 Avatar Lab Isolated\",\n    currentUrl: state.currentUrl,\n    hasVRM: !!state.currentVRM,\n    selectedMotion: els.motionSelect.value || \"\",\n    sync: \"BroadcastChannel noelle-avatar-room-sync + localStorage noelle.avatar.sync.state\",\n    documentBase: document.baseURI,\n    note: \"Se caminho local falhar, use input de arquivo local.\"\n  };\n\n  navigator.clipboard?.writeText(JSON.stringify(report, null, 2));\n  setStatus(\"Relat\u00f3rio copiado\", \"ok\");\n}\n\nfunction animate() {\n  const dt = Math.min(0.05, clock.getDelta());\n\n  if (state.currentVRM?.update) {\n    try { state.currentVRM.update(dt); } catch {}\n  }\n  if (state.mixer) {\n    try { state.mixer.update(dt); } catch {}\n  }\n\n  if (state.currentRoot && !state.mixer) {\n    state.idleTime += dt;\n    state.currentRoot.position.y = Math.sin(state.idleTime * 2.0) * 0.012;\n  }\n\n  controls.update();\n  renderer.render(scene, camera);\n  requestAnimationFrame(animate);\n}\n\nels.btnLoad.addEventListener(\"click\", async () => {\n  try {\n    if (els.localFile.files?.[0]) {\n      const file = els.localFile.files[0];\n      const objectUrl = URL.createObjectURL(file);\n      state.objectUrls.push(objectUrl);\n      await loadAvatar(objectUrl, file.name);\n      return;\n    }\n    await loadAvatar(els.avatarSelect.value, els.avatarSelect.value);\n  } catch (err) {\n    console.error(err);\n    setStatus(`Falha ao carregar avatar: ${err.message}`, \"danger\");\n    log(err.stack || err.message);\n  }\n});\n\nels.localFile.addEventListener(\"change\", () => {\n  if (els.localFile.files?.[0]) {\n    setStatus(`Arquivo local selecionado: ${els.localFile.files[0].name}`, \"warn\");\n  }\n});\n\nels.avatarSelect.addEventListener(\"change\", () => {\n  els.localFile.value = \"\";\n});\n\nels.btnResetCamera.addEventListener(\"click\", resetCamera);\nels.zoomRange.addEventListener(\"input\", resetCamera);\nels.btnIdle.addEventListener(\"click\", () => {\n  stopMotion();\n  setExpression(\"relaxed\", 0.25);\n  postSync(\"state:idle\", { url: state.currentUrl });\n});\nels.btnBlink.addEventListener(\"click\", () => setExpression(\"blink\", 1));\nels.btnHappy.addEventListener(\"click\", () => setExpression(\"happy\", 1));\nels.btnNeutral.addEventListener(\"click\", () => setExpression(\"neutral\", 0));\nels.btnPlayMotion.addEventListener(\"click\", () => {\n  playMotion(els.motionSelect.value).catch((err) => {\n    console.error(err);\n    setStatus(`Erro VRMA: ${err.message}`, \"danger\");\n    log(err.stack || err.message);\n  });\n});\nels.btnStopMotion.addEventListener(\"click\", stopMotion);\nels.btnSyncRoom.addEventListener(\"click\", () => {\n  postSync(\"avatar:sync-room\", {\n    url: state.currentUrl,\n    motion: els.motionSelect.value || \"\",\n    hasVRM: !!state.currentVRM\n  });\n  setStatus(\"Estado enviado para Room\", \"ok\");\n});\nels.btnCopyReport.addEventListener(\"click\", copyReport);\n\nwindow.addEventListener(\"beforeunload\", () => {\n  for (const url of state.objectUrls) {\n    try { URL.revokeObjectURL(url); } catch {}\n  }\n});\n\nawait loadMotionManifest();\nawait loadAvatar(els.avatarSelect.value, els.avatarSelect.value).catch((err) => {\n  console.error(err);\n  setStatus(`Falha ao carregar avatar por caminho: ${err.message}. Use arquivo local.`, \"danger\");\n  log(err.stack || err.message);\n});\n\nresetCamera();\nanimate();\n",
  launcherJs: "(() => {\n  \"use strict\";\n\n  const BUTTON_ID = \"noelle-avatar-lab-v19-6-button\";\n  const STYLE_ID = \"noelle-avatar-lab-v19-6-launcher-style\";\n\n  function injectStyle() {\n    if (document.getElementById(STYLE_ID)) return;\n    const style = document.createElement(\"style\");\n    style.id = STYLE_ID;\n    style.textContent = `\n      #${BUTTON_ID} {\n        position: fixed;\n        right: 18px;\n        bottom: 132px;\n        z-index: 2147483300;\n        border: 1px solid rgba(255,83,136,.52);\n        border-radius: 999px;\n        padding: 10px 14px;\n        color: #fff;\n        background: linear-gradient(135deg,#ff477e,#8b5cf6);\n        box-shadow: 0 14px 40px rgba(0,0,0,.34);\n        font-weight: 900;\n        cursor: pointer;\n      }\n    `;\n    document.head.appendChild(style);\n  }\n\n  function openLab() {\n    const url = new URL(\"./avatar_lab_v19_6.html\", document.baseURI).href;\n    window.open(url, \"noelle-avatar-lab-v19-6\", \"width=1180,height=780\");\n  }\n\n  function start() {\n    if (document.getElementById(BUTTON_ID)) return;\n    injectStyle();\n    const btn = document.createElement(\"button\");\n    btn.id = BUTTON_ID;\n    btn.type = \"button\";\n    btn.textContent = \"\ud83e\uddea Avatar Lab\";\n    btn.title = \"Abrir Noelle Avatar Lab V19.6\";\n    btn.addEventListener(\"click\", openLab);\n    document.body.appendChild(btn);\n  }\n\n  if (document.readyState === \"loading\") document.addEventListener(\"DOMContentLoaded\", start);\n  else start();\n\n  window.NoelleAvatarLabV196 = { open: openLab };\n})();",
  roomSyncBridge: "(() => {\n  \"use strict\";\n\n  const PATCH_ID = \"NOELLE_V19_6_ROOM_SYNC_BRIDGE_2026\";\n  const PILL_ID = \"noelle-v19-6-room-sync-pill\";\n  const STYLE_ID = \"noelle-v19-6-room-sync-style\";\n\n  function injectStyle() {\n    if (document.getElementById(STYLE_ID)) return;\n    const style = document.createElement(\"style\");\n    style.id = STYLE_ID;\n    style.textContent = `\n      #${PILL_ID} {\n        position: fixed;\n        right: 18px;\n        bottom: 82px;\n        z-index: 2147483200;\n        border: 1px solid rgba(255,83,136,.42);\n        border-radius: 999px;\n        padding: 8px 12px;\n        color: #fff4fb;\n        background: rgba(14,14,25,.90);\n        box-shadow: 0 14px 38px rgba(0,0,0,.32);\n        font-weight: 800;\n        font-size: 13px;\n        max-width: 360px;\n        white-space: nowrap;\n        overflow: hidden;\n        text-overflow: ellipsis;\n      }\n    `;\n    document.head.appendChild(style);\n  }\n\n  function setPill(text) {\n    injectStyle();\n    let el = document.getElementById(PILL_ID);\n    if (!el) {\n      el = document.createElement(\"div\");\n      el.id = PILL_ID;\n      document.body.appendChild(el);\n    }\n    el.textContent = text;\n  }\n\n  function consume(message) {\n    if (!message || (message.source !== \"avatar-lab-v19-6\" && message.source !== \"avatar-v19-5\")) return;\n\n    try {\n      localStorage.setItem(\"noelle.room.avatar.sync.last\", JSON.stringify(message, null, 2));\n    } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"noelle:room-avatar-sync\", { detail: message }));\n    } catch {}\n\n    const payload = message.payload || {};\n    const url = payload.url || payload.motion || payload.name || \"estado\";\n\n    if (message.type === \"avatar:loaded\" || message.type === \"avatar:sync-room\") {\n      setPill(`Avatar sync: ${String(url).split(\"/\").pop()}`);\n      try { window.noelleRoomPlayer?.setAvatar?.(payload.url); } catch {}\n      try { window.roomPlayerApi?.setAvatar?.(payload.url); } catch {}\n      try { window.noelleRoomV19?.setAvatar?.(payload.url); } catch {}\n    }\n\n    if (message.type === \"motion:play\") {\n      setPill(`Motion sync: ${String(url).split(\"/\").pop()}`);\n      try { window.noelleRoomPlayer?.playMotion?.(payload.url); } catch {}\n      try { window.roomPlayerApi?.playMotion?.(payload.url); } catch {}\n    }\n\n    if (message.type === \"expression:set\") {\n      setPill(`Expression sync: ${payload.name || \"expression\"}`);\n      try { window.noelleRoomPlayer?.setExpression?.(payload.name, payload.value); } catch {}\n      try { window.roomPlayerApi?.setExpression?.(payload.name, payload.value); } catch {}\n    }\n\n    if (message.type === \"motion:stop\") {\n      setPill(\"Motion sync: parada\");\n      try { window.noelleRoomPlayer?.stopMotion?.(); } catch {}\n      try { window.roomPlayerApi?.stopMotion?.(); } catch {}\n    }\n  }\n\n  function start() {\n    if (window.__NOELLE_V19_6_ROOM_SYNC_STARTED__) return;\n    window.__NOELLE_V19_6_ROOM_SYNC_STARTED__ = true;\n\n    setPill(\"Avatar sync pronto\");\n\n    try {\n      const ch = new BroadcastChannel(\"noelle-avatar-room-sync\");\n      ch.onmessage = (event) => consume(event.data);\n    } catch {\n      setPill(\"Avatar sync: BroadcastChannel indispon\u00edvel\");\n    }\n\n    window.addEventListener(\"storage\", (event) => {\n      if (event.key !== \"noelle.avatar.sync.state\" || !event.newValue) return;\n      try { consume(JSON.parse(event.newValue)); } catch {}\n    });\n\n    console.log(\"[Noelle V19.6] Room Sync Bridge ativo\");\n  }\n\n  if (document.readyState === \"loading\") document.addEventListener(\"DOMContentLoaded\", start);\n  else start();\n\n  window.__NOELLE_V19_6_ROOM_SYNC_BRIDGE__ = PATCH_ID;\n})();",
  buildScript: "\"use strict\";\n\nconst fs = require(\"fs\");\nconst path = require(\"path\");\n\nconst ROOT = process.cwd();\n\nasync function main() {\n  let esbuild;\n  try {\n    esbuild = require(\"esbuild\");\n  } catch {\n    console.error(\"[ERRO] esbuild n\u00e3o encontrado. Rode npm install.\");\n    process.exit(1);\n  }\n\n  const entry = path.join(ROOT, \"src\", \"renderer\", \"avatar_lab_v19_6_app.js\");\n  const outdir = path.join(ROOT, \"src\", \"renderer_dist\");\n  const outfile = path.join(outdir, \"avatar_lab_v19_6.bundle.js\");\n\n  if (!fs.existsSync(entry)) {\n    console.error(\"[ERRO] Entrada n\u00e3o encontrada:\", entry);\n    process.exit(1);\n  }\n\n  fs.mkdirSync(outdir, { recursive: true });\n\n  await esbuild.build({\n    entryPoints: [entry],\n    outfile,\n    bundle: true,\n    format: \"iife\",\n    platform: \"browser\",\n    target: [\"chrome120\"],\n    sourcemap: true,\n    logLevel: \"info\"\n  });\n\n  console.log(\"[OK] Bundle Avatar Lab V19.6 gerado:\", path.relative(ROOT, outfile));\n}\n\nmain().catch((err) => {\n  console.error(err);\n  process.exit(1);\n});\n",
  memorySection: "\n## V19.6 Avatar Lab Isolated \u2014 2026\n\nCorre\u00e7\u00e3o da estrat\u00e9gia V19.5:\n- O V19.5 injetado no app principal quebrou troca de abas.\n- A solu\u00e7\u00e3o correta \u00e9 isolar o preview real do VRM em uma janela separada: `src/avatar_lab_v19_6.html`.\n\nEste pack adiciona os quatro itens solicitados sem mexer diretamente na navega\u00e7\u00e3o de abas:\n1. Preview real do VRM em janela isolada.\n2. Loader novo com Three.js + @pixiv/three-vrm.\n3. Sincroniza\u00e7\u00e3o com Room via BroadcastChannel/localStorage/CustomEvent.\n4. Anima\u00e7\u00f5es VRMA via @pixiv/three-vrm-animation.\n\nArquivos principais:\n- `src/avatar_lab_v19_6.html`\n- `src/renderer/avatar_lab_v19_6_app.js`\n- `src/renderer/avatar_lab_launcher_v19_6.js`\n- `src/renderer/room_sync_bridge_v19_6.js`\n- `scripts/build_avatar_lab_v19_6_2026.cjs`\n\nImportante:\n- A aba principal n\u00e3o recebe MutationObserver global.\n- Se caminho `./assets/Noelle.vrm` falhar, o Avatar Lab permite escolher VRM/GLB local por input de arquivo.\n",
  docs: "# Noelle V19.6 \u2014 Avatar Lab Isolated\n\nEste pack adiciona os quatro itens que voc\u00ea pediu sem quebrar a troca de abas:\n\n1. Preview real do VRM.\n2. Loader novo do avatar/widget.\n3. Sincroniza\u00e7\u00e3o real com Room.\n4. Anima\u00e7\u00f5es VRM/VRMA.\n\n## Por que agora \u00e9 isolado?\n\nO V19.5 quebrou a mudan\u00e7a de abas porque injetava o preview diretamente no app principal.\nA V19.6 cria uma janela separada:\n\n```txt\nsrc/avatar_lab_v19_6.html\n```\n\nAssim o preview real do VRM n\u00e3o briga com a navega\u00e7\u00e3o do app principal.\n\n## Como abrir\n\nDepois de aplicar, aparece um bot\u00e3o flutuante:\n\n```txt\n\ud83e\uddea Avatar Lab\n```\n\nOu abra direto:\n\n```txt\nsrc/avatar_lab_v19_6.html\n```\n\n## Se der Failed to fetch\n\nUse o campo:\n\n```txt\nOu carregar VRM/GLB local\n```\n\nIsso usa `URL.createObjectURL(file)` e evita problema de caminho relativo em `file://`.\n\n## Sincroniza\u00e7\u00e3o com Room\n\nO Avatar Lab envia:\n\n```txt\nBroadcastChannel: noelle-avatar-room-sync\nlocalStorage: noelle.avatar.sync.state\nCustomEvent: noelle:avatar-sync\n```\n\nA Room recebe pela bridge:\n\n```txt\nsrc/renderer/room_sync_bridge_v19_6.js\n```\n\n## N\u00e3o entra neste pack\n\nNada dos quatro itens foi deixado de fora.\n\nLimite conhecido:\n- A sincroniza\u00e7\u00e3o visual profunda depende de a Room expor APIs internas do player. O bridge tenta nomes comuns e tamb\u00e9m dispara evento `noelle:room-avatar-sync`.\n"
};

function abs(rel) { return path.join(ROOT, rel); }
function exists(rel) { return fs.existsSync(abs(rel)); }
function read(rel) { return fs.readFileSync(abs(rel), "utf8"); }
function write(rel, text) { fs.mkdirSync(path.dirname(abs(rel)), { recursive: true }); fs.writeFileSync(abs(rel), text, "utf8"); }
function log(msg) { console.log(msg); }
function warn(msg) { console.log("[AVISO] " + msg); }
function fail(msg) { console.error("[ERRO] " + msg); process.exitCode = 1; }

function backup(rel) {
  if (!exists(rel)) return;
  const dst = path.join(BACKUP_ROOT, rel);
  fs.mkdirSync(path.dirname(dst), { recursive: true });
  fs.copyFileSync(abs(rel), dst);
}

function backupAndWrite(rel, text) {
  backup(rel);
  write(rel, text);
  log("[OK] Atualizado: " + rel);
}

function nodeCheck(rel) {
  if (!exists(rel)) return true;
  const result = cp.spawnSync(process.execPath, ["--check", abs(rel)], { encoding: "utf8" });
  if (result.status !== 0) {
    fail("node --check falhou: " + rel);
    if (result.stdout) console.error(result.stdout);
    if (result.stderr) console.error(result.stderr);
    return false;
  }
  log("[OK] node --check " + rel);
  return true;
}

function addScriptToHtml(rel, scriptSrc) {
  if (!exists(rel)) return warn(rel + " não encontrado.");
  let html = read(rel);
  const escaped = scriptSrc.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  html = html.replace(new RegExp(`\\n?\\s*<script\\s+src=["']${escaped}["']><\\/script>\\s*`, "g"), "\n");
  const tag = `<script src="${scriptSrc}"></script>`;
  if (html.includes("</body>")) html = html.replace("</body>", `  ${tag}\n</body>`);
  else html += `\n${tag}\n`;
  backupAndWrite(rel, html);
}

function patchPackage() {
  const rel = "package.json";
  if (!exists(rel)) return warn("package.json não encontrado.");
  let pkg;
  try { pkg = JSON.parse(read(rel)); }
  catch (err) { return warn("package.json inválido: " + err.message); }

  backup(rel);
  pkg.version = "19.6.0-avatar-lab-isolated-2026";
  pkg.scripts = {
    ...(pkg.scripts || {}),
    "build:avatar-lab-v19.6": "node scripts/build_avatar_lab_v19_6_2026.cjs",
    "apply:v19.6-avatar-lab": "node scripts/apply_v19_6_avatar_lab_isolated_2026.cjs --apply",
    "diagnostico:v19.6-avatar-lab": "node scripts/diagnostico_v19_6_avatar_lab_isolated_2026.cjs"
  };
  pkg.dependencies = { ...(pkg.dependencies || {}) };
  if (!pkg.dependencies.three) pkg.dependencies.three = "^0.180.0";
  if (!pkg.dependencies["@pixiv/three-vrm"]) pkg.dependencies["@pixiv/three-vrm"] = "^3.5.2";
  if (!pkg.dependencies["@pixiv/three-vrm-animation"]) pkg.dependencies["@pixiv/three-vrm-animation"] = "^3.5.2";
  pkg.devDependencies = { ...(pkg.devDependencies || {}) };
  if (!pkg.devDependencies.esbuild) pkg.devDependencies.esbuild = "^0.25.0";

  write(rel, JSON.stringify(pkg, null, 2) + "\n");
  log("[OK] package.json atualizado.");
}

function patchMemory() {
  const rel = "MEMORIA_GPT_NOELLE.md";
  if (!exists(rel)) {
    backupAndWrite(rel, "# MEMORIA_GPT_NOELLE\n\n" + FILES.memorySection.trim() + "\n");
    return;
  }
  let text = read(rel);
  if (!text.includes("V19.6 Avatar Lab Isolated")) {
    text = text.trimEnd() + "\n\n" + FILES.memorySection.trim() + "\n";
    backupAndWrite(rel, text);
  } else {
    log("[OK] MEMORIA_GPT_NOELLE.md já contém V19.6.");
  }
}

function tryBuild() {
  const result = cp.spawnSync(process.execPath, ["scripts/build_avatar_lab_v19_6_2026.cjs"], { cwd: ROOT, encoding: "utf8" });
  if (result.status === 0) {
    if (result.stdout) process.stdout.write(result.stdout);
    if (result.stderr) process.stderr.write(result.stderr);
  } else {
    warn("Bundle ainda não foi gerado. Rode: npm install && npm run build:avatar-lab-v19.6");
    if (result.stdout) process.stdout.write(result.stdout);
    if (result.stderr) process.stderr.write(result.stderr);
  }
}

function apply() {
  fs.mkdirSync(BACKUP_ROOT, { recursive: true });
  log("[INFO] Backup em: " + BACKUP_ROOT);

  backupAndWrite("src/avatar_lab_v19_6.html", FILES.avatarLabHtml);
  backupAndWrite("src/renderer/avatar_lab_v19_6_app.js", FILES.avatarLabApp);
  backupAndWrite("src/renderer/avatar_lab_launcher_v19_6.js", FILES.launcherJs);
  backupAndWrite("src/renderer/room_sync_bridge_v19_6.js", FILES.roomSyncBridge);
  backupAndWrite("scripts/build_avatar_lab_v19_6_2026.cjs", FILES.buildScript);
  backupAndWrite("docs/NOELLE_V19_6_AVATAR_LAB_ISOLATED.md", FILES.docs);

  addScriptToHtml("src/controls.html", "./renderer/avatar_lab_launcher_v19_6.js");
  addScriptToHtml("src/room.html", "./renderer/room_sync_bridge_v19_6.js");

  patchPackage();
  patchMemory();

  for (const rel of [
    "src/renderer/avatar_lab_v19_6_app.js",
    "src/renderer/avatar_lab_launcher_v19_6.js",
    "src/renderer/room_sync_bridge_v19_6.js",
    "scripts/build_avatar_lab_v19_6_2026.cjs",
    "scripts/diagnostico_v19_6_avatar_lab_isolated_2026.cjs"
  ]) nodeCheck(rel);

  tryBuild();

  log("");
  if (process.exitCode) log("[RESULTADO] Aplicou parcialmente, mas há erro acima.");
  else log("[OK] V19.6 Avatar Lab Isolated aplicado.");
}

if (process.argv.includes("--apply")) apply();
else console.log("Uso: node scripts/apply_v19_6_avatar_lab_isolated_2026.cjs --apply");


// V19.7.3: remendo defensivo para manter build iife valido.
try { require('child_process').spawnSync(process.execPath, ['scripts/fix_v19_7_3_build_regex_2026.cjs', '--ensure'], { cwd: process.cwd(), stdio: 'inherit' }); } catch {}
